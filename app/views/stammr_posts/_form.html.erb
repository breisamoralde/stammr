<%= form_for(@stammr_post) do |f| %>
  <% if @stammr_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stammr_post.errors.count, "error") %> prohibited this stammr_post from being saved:</h2>

      <ul>
      <% @stammr_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
